# Scenario Coverage Matrix

| # | Scenario | Simulation | Detection | Handling Strategy | Evidence |
|---|----------|-----------|-----------|-------------------|----------|
| 1 | Popup/modal | Overdue reminder popup rendered on listing page, shows random book title | `page.locator("#newsletter-popup")` checked for visibility on every page load | Click `#close-popup` to dismiss, wait for hidden state before proceeding | Bot run log: "Detected popup — dismissing" / "Popup dismissed" — 8 occurrences in successful 3-item run |
| 2 | Cookie/consent banner | Bottom banner rendered on listing page | `page.locator("#cookie-banner")` checked for visibility on every page load | Click `#accept-cookies` to dismiss, wait for hidden state before proceeding | Bot run log: "Detected cookie banner — accepting" / "Cookie banner accepted" — 8 occurrences in successful 3-item run |

| 3 | Simulated captcha gate | Riddle-based challenge page shown before book detail page | `page.locator("input[name='correct_answer']")` checked for presence (hidden field carries the expected answer) | Read the hidden answer value, fill it into the visible answer field, submit — solved programmatically rather than pausing for a human, since the answer is deterministically derivable | Bot run log: "Detected captcha gate — solving" / "Captcha solved, proceeding" — confirmed across multiple runs, including chained retriggers after redirects |
| 4 | Site down / server errors | Listing and detail pages randomly return 503 "Internal Server Error (simulated)" | `is_error_page()` checks page content for "Internal Server Error" text after every navigation | Retry with exponential backoff (2s, 4s, 8s... capped at 10s), up to 5 attempts, before giving up on that step | Bot run log: "Server error during '...' — retrying in Xs (attempt N/5)" followed by successful recovery — confirmed across multiple runs |
| 5 | Slow responses / timeouts | Random 2-5s delays on page loads | Covered implicitly — bot uses `wait_for_load_state("networkidle")` and Playwright's built-in action timeouts instead of fixed sleeps | No special handler needed; proper waits naturally absorb delays | Bot run log shows longer gaps between some steps (e.g. 5s+ between actions) without errors |
| 6 | Unexpected redirection | Book detail clicks sometimes show a promo page instead, on the same URL | `page.locator("text=Limited Time Offer")` checked in the recovery loop | Click "No thanks, take me back" link, then re-check for other disruptions before proceeding | Bot run log: "Detected redirect to promo page — routing back" — recovered successfully in multiple runs, including one case chained with a captcha retrigger |
| 7 | DOM change / selector drift | Listing page randomly renders an alternate template (different tags, classes, structure) for the same book data, based on probability in chaos.json | *(bot detection to be built)* | *(bot detection to be built)* | Confirmed both layouts render correctly with identical data; alternate layout uses different HTML structure entirely |
| 8 | Blocked / intercepted clicks | A sticky "Flash Sale" overlay randomly appears over the Request to Borrow button on the detail page, based on probability in chaos.json | *(bot detection to be built)* | *(bot detection to be built)* | Confirmed overlay blocks the button underneath; dismissing overlay via its X button restores normal clicking |
**Update:** Happy-path bot is now working end-to-end (chaos disabled). Detection/handling columns below will be filled in as each scenario's bot-side handler is built.